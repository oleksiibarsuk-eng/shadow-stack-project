name: Agent HQ

on:
  issues:
    types: [labeled]

jobs:
  agent:
    if: contains(github.event.issue.labels.*.name, 'agent-ready')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Agent processing
        run: echo "Agent HQ triggered"
